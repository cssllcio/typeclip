import XCTest

// Deleted in Task 2. Gives the test target a compilable source so the full
// build+test pipeline is proven in CI from the first commit; deliberately
// declares no test methods (a placeholder assertion would be review noise).
final class ScaffoldTests: XCTestCase {}
